// import { ADD_USER } from "../actionTypes";

const initialState = {
  users: [],
};

export default function clock(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
